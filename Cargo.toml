[package]
name = "plato-tile-spec"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
description = "Unified tile specification for the Plato knowledge system"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
